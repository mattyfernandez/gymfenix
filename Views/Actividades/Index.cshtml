@model IEnumerable<GimnasioFenix.Models.Actividad>

@{
    ViewBag.Title = "Index";
}

<h2>Actividades</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Actividad1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dia.Dia1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horario.Horario1)
        </th>
        <th>
            Profesor
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cupos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Actividad1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dia.Dia1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horario.Horario1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cupos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
